<!DOCTYPE html>
<head>
    <title>Delete a Book</title>
    <link rel="stylesheet" href="/style">
</head>
<html>
    <body>
        <div class="container">
            <h2>To delete a book, click on the red X button.</h2>
            <table class="bookContainer">
                <% books.forEach (s=>
                { %>
                <form action="<%= `home/${s._id}/delete?_method=DELETE` %>" method="POST">
                    <tr>
                        <td class="book alignLeft">
                            <button class="deleteButton" type="submit" onclick="return confirm('Are you sure you want to delete this record?')">X</button>
                            <label><%= s.bookName%></label>
                        </td>
                    </tr>
                </form>
                <% });
                %>
                
            </table>
            <div class="nav">
                <hr>  
                <a href="../home">List of Books</a><span class="navPipe">|</span>
                <a href="../AddNewBook">Add a Book</a><span class="navPipe">|</span>
                <a href="../DeleteABook">Delete a Book</a>
            </div>
        </div>
    </body>
</html>